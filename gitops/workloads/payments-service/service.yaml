apiVersion: v1
kind: Service
metadata:
  name: payments-service
  labels:
    app.kubernetes.io/name: payments-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payments-service
  ports:
    - name: http
      port: 80
      targetPort: http
