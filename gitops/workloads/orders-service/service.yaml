apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app.kubernetes.io/name: orders-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orders-service
  ports:
    - name: http
      port: 80
      targetPort: http
